# Swift Package Manager build artifacts
.build/
.swiftpm/

# Packaged .app output (rebuilt via Scripts/build_app.sh)
build/

# Packaged .dmg output (rebuilt via Scripts/build_dmg.sh; published as GitHub
# Release assets, not committed to the repo)
dist/

# macOS
.DS_Store

# Xcode (not used here, but harmless to ignore if it ever shows up)
DerivedData/
*.xcuserstate

# .NET build artifacts (windows/ — the Windows port)
bin/
obj/
*.user
